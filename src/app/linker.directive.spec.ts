import { LinkerDirective } from './linker.directive';

describe('LinkerDirective', () => {
  it('should create an instance', () => {
    const directive = new LinkerDirective();
    expect(directive).toBeTruthy();
  });
});
